# Models Directory
Place custom fine-tuned model checkpoints here (e.g. `food101_efficientnet_b4.pth`).
If not provided, the vision server defaults to torchvision pretrained weights with heuristic fallback.
